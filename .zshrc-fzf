## AUTOGENERATED
## fzf examples - completion
# https://github.com/junegunn/fzf/wiki/Examples-(completion)

_fzf_complete_brew() {
  ARGS="$@"
  if [[ $ARGS == 'brew install '* ]]; then
    local options="$(cache_fzf.js "brew install")"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  elif [[ $ARGS == 'brew cask install '* ]]; then
    local options="$(cache_fzf.js "brew cask install")"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  elif [[ $ARGS == 'brew uninstall '* ]]; then
    local options="$(brew list)"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  elif [[ $ARGS == 'brew cask uninstall '* ]]; then
    local options="$(brew cask list)"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  else
    return 1
  fi
}
_fzf_complete_git() {
  ARGS="$@"
  if [[ $ARGS == 'git add '* ]]; then
    local options="$(cache_fzf.js "git co")"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  elif [[ $ARGS == 'git co '* ]]; then
    local options="$(cache_fzf.js "git co")"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  elif [[ $ARGS == 'git cob '* ]]; then
    local options="$(git for-each-ref --sort=-committerdate refs/heads/ --format='%(refname:short)')"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  else
    return 1
  fi
}
_fzf_complete_yarn() {
  ARGS="$@"
  if [[ $ARGS == 'yarn test'* ]]; then
    local options="$(rg --files --hidden --glob '!.git' -g '*.test.ts' -g '*.test.js' -g '*.it.ts' -g '*.it.js' 2> /dev/null)"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  else
    return 1
  fi
}
_fzf_complete_node() {
  ARGS="$@"
  if [[ $ARGS == 'node '* ]]; then
    local options="$(rg --files --hidden --glob '!.git' -g '*.ts' -g '*.js' 2> /dev/null)"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  else
    return 1
  fi
}
_fzf_complete_npx() {
  ARGS="$@"
  if [[ $ARGS == 'npx gulp '* ]]; then
    local options="$(cache_fzf.js 'npx gulp')"
    _fzf_complete "--multi" "$@" < <(
      echo $options
    )
  else
    return 1
  fi
}